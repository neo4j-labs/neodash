import React from 'react';
import { ChartProps } from '../../../chart/Chart';
/**
 * Renders a form.
 */
const NeoForm = (props: ChartProps) => {
  const { records, settings } = props;
  return <div style={{ marginTop: '0px' }}>Hello!</div>;
};

export default NeoForm;
